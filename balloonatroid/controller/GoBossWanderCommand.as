/* PureMVC AS2 FlashLite Demo - Balloonatroid Copyright (c) 2007, 2008 by Cliff Hall <clifford.hall@puremvc.org> and  Chandima Cumaranatunge <chandima.cumaranatunge@puremvc.org> Your reuse is governed by the Creative Commons Attribution 3.0 License */import org.puremvc.as2.interfaces.ICommand;import org.puremvc.as2.interfaces.INotification;import org.puremvc.as2.patterns.command.SimpleCommand;import balloonatroid.*;import balloonatroid.view.*;import balloonatroid.model.*;class balloonatroid.controller.GoBossWanderCommand extends SimpleCommand implements ICommand{	/**	 * Change boss balloon state to the WANDER State.	 * Retrives the <code>BossProxy</code> proxy and changes the state to	 * <code>BossProxy.WANDER</code> singleton state instance.	 *	 * @see balloonatroid.model.BossProxy	 * @see balloonatroid.model.boss.WanderState	 * @use Does not utilize notification body.	 * @use <code>BossProxy</code> needs to be registered prior to command execution.	 */	public function execute( note:INotification ) : Void 	{				// Get the BossProxy proxy		var bossProxy = facade.retrieveProxy(BossProxy.NAME);		// Change the boss state 		bossProxy.changeState(BossProxy.WANDER);	}}