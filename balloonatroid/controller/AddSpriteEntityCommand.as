/* PureMVC AS2 FlashLite Demo - Balloonatroid Copyright (c) 2007, 2008 by Cliff Hall <clifford.hall@puremvc.org> and  Chandima Cumaranatunge <chandima.cumaranatunge@puremvc.org> Your reuse is governed by the Creative Commons Attribution 3.0 License */import org.puremvc.as2.interfaces.ICommand;import org.puremvc.as2.interfaces.INotification;import org.puremvc.as2.patterns.command.SimpleCommand;import balloonatroid.*;import balloonatroid.view.*;import balloonatroid.model.*;import balloonatroid.interfaces.IState;import balloonatroid.view.*;import mx.utils.*;class balloonatroid.controller.AddSpriteEntityCommand extends SimpleCommand implements ICommand{	/**	 * Add sprite to sprite entities collection for hit testing.	 *	 * Usually called from sprite mediator during initialization. Retrieves the 	 * <code>SpriteEntitiesProxy</code> and adds the sprite passed in the 	 * note body to the sprite entities collection. The sprite should 	 * subclass <code>AbstractSpriteDO</code>. 	 *	 * @param note Notification with type AbstractSpriteDO object as body	 * @see balloonatroid.model.SpriteEntitiesProxy	 * @see balloonatroid.model.AbstractSpriteDO	 * @use <code>SpriteEntitiesProxy</code> needs to be registered prior to command execution	 */	public function execute( note:INotification ) : Void 	{				// get sprite entities proxy		var spriteEntitiesProxy = SpriteEntitiesProxy( facade.retrieveProxy( SpriteEntitiesProxy.NAME ) ); 		// get the sprite and cast it to AbstractSpriteDO		var spriteEntity:AbstractSpriteDO = AbstractSpriteDO( note.getBody() ); 		// Add sprite entity to collection		spriteEntitiesProxy.addSprite( spriteEntity ); 	}}